# 2026-04-11

- In Telegram group chat, Kolade corrected the Orisynx backend/frontend tracking references and asked Jarvis to review the repos and update logs.
- Initial browser-only checks against GitHub incorrectly suggested no access because the repos are private and unauthenticated web access returned `Page not found`.
- Follow-up authenticated GitHub CLI verification on host confirmed active access via `gh` as account `koladeg` using `GH_TOKEN`.
- Confirmed private repo access with `viewerPermission: ADMIN` for:
  - `Orisynx/backend`
  - `Orisynx/frontend`
  - `Orisynx/bootstrap`
  - `Orisynx/Orisynx-backend`
- Current Orisynx org inventory visible via authenticated CLI (all private):
  - `Orisynx/backend`
  - `Orisynx/frontend`
  - `Orisynx/bootstrap`
  - `Orisynx/demo-repository`
  - `Orisynx/Orisynx-backend`
  - `Orisynx/Old-Orisynx-backend`
  - `Orisynx/audit`
- Durable tracking update from Kolade: the main repos to monitor going forward are:
  - `Orisynx/backend`
  - `Orisynx/frontend`
  - `Orisynx/bootstrap`
- Latest visible main-branch commit baselines captured on 2026-04-11 UTC:
  - `Orisynx/backend` → `9c46d48` (`chore: add types script, pre-commit check, and CI step (#14)`) — pushed 2026-04-10 22:12 UTC
  - `Orisynx/frontend` → `d7c73b0` (`feat: wire detail pages and forms to live GraphQL backend (#14)`) — pushed 2026-04-10 21:41 UTC
  - `Orisynx/bootstrap` → `bd2973d` (`chore: fix TypeScript errors in bootstrap (#11)`) — pushed 2026-04-06 06:16 UTC
- Architecture comparison note:
  - `Orisynx/backend` is the newer NestJS + GraphQL + MongoDB backend and should be treated as the primary backend repo.
  - `Orisynx/Orisynx-backend` appears to be an older Express-based backend with latest visible commit `060a219` from 2026-03-12 and should be treated as legacy/secondary unless Kolade says otherwise.
- Root-level repo shape check:
  - `backend`: NestJS service with `src/`, `test/`, Dockerfiles, GitHub workflows, docs, env example.
  - `frontend`: Next.js app with `app/`, `components/`, `e2e/`, Playwright/Vitest, Docker compose files.
  - `bootstrap`: infra/local orchestration repo for coordinating sibling `backend` + `frontend` repos, with CLI/setup scripts and Docker compose.
- Frontend review snapshot (2026-04-11):
  - `Orisynx/frontend` is a substantial product shell, not just a blank starter.
  - Current implemented shape includes auth pages, dashboard, audit pages, findings, CAPA, tasks, templates, users, notifications, reports, history, analytics, calendar, profile, framework creation route, shared layout/components, Apollo client, auth context, GraphQL query/mutation modules, REST helper layer, IndexedDB layer, unit tests, and Playwright e2e scaffolding.
  - The repo is in a transition state from earlier local/mock/IndexedDB behavior toward live backend integration; this is visible from coexistence of `lib/db*` local persistence, `lib/api-*` REST wrappers, and `lib/graphql/*` + Apollo integration.
  - Durable frontend risks worth remembering:
    - `next.config.mjs` currently ignores TypeScript build errors and ESLint errors during builds.
    - Auth currently stores tokens in `localStorage` and non-HttpOnly cookies, acceptable for MVP speed but not production-safe.
    - Documentation is partially stale/inconsistent (README still references older backend assumptions and IndexedDB-heavy setup while repo now has GraphQL integration work).
  - Current-vs-later product framing for frontend:
    - Current: strong desktop-first UI shell with many screens and significant module coverage.
    - Current: active wiring into live backend/auth for core flows.
    - Current: some legacy mock/local-data paths still present.
    - Later: stronger RBAC enforcement, real workflow/state rules, evidence/file storage, immutable audit trail, notifications delivery, planner/template activation logic, reports/export depth, and richer team/workspace behaviors.
- Durable lesson reinforced: do not infer private GitHub repo access from unauthenticated browser 404s; verify with authenticated `gh`/API before concluding access status.
