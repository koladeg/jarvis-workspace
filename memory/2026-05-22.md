- Investigated the two-day silence after Kolade reported missed check-ins. The cron scheduler was still alive, but several jobs were failing for two separate reasons: `ollama/llama3.2:3b` was no longer allowed by the cron model allowlist, and some OpenAI/Anthropic jobs had recent provider/network failures.
- Updated `/home/claw/.openclaw/cron/jobs.json` to move the blocked Ollama-backed cron jobs onto `openai/gpt-5.1` and normalized the biweekly pipeline review job from the vague `gpt` alias to explicit `openai/gpt-5.4`.
- Checked Robin directly. The local listener was running, webhook state was empty, and `pending_update_count` returned to `0` after a controlled local restart.
- Hardened `scripts/research-bot-listener.py` with a local single-instance lock plus clearer Telegram GET error reporting. Verified that a second local listener instance is now rejected cleanly with `another local Robin listener instance is already running`.
- Robin logs showed historical `HTTP 409 Conflict`, which still indicates a competing poller lesson remains valid, but after the local restart the active listener resumed normal `No new updates` / processed-update behavior without fresh conflict evidence in the immediate check.
- Investigated a later Robin failure reported by Kolade on May 22, 2026 UTC: Robin tried to run a heavy multi-lane request through the normal foreground reply path and timed out after the listener's `300` second cap (`330` seconds wall-clock with buffer). Added a background-run path so long lane requests are acknowledged immediately and executed asynchronously via `scripts/research-bot-runner.py` with a longer timeout.
- Removed the local `n8n` installation from the server on May 22, 2026 UTC. There was no active `n8n` process, no Docker install, and no systemd unit keeping it alive; the install was a global npm package plus `scripts/start-n8n.sh`. Uninstalled `n8n`, moved the launcher into `/home/claw/.openclaw/workspace/removed/n8n-20260522T190242Z`, and verified `which n8n` is empty, `~/.n8n` is gone, and nothing is listening on port `5678`.
