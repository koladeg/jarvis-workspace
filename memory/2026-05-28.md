- Verified on 2026-05-28 that GitHub CLI auth is currently broken on this machine: `gh auth status` reported `Failed to log in to github.com using token (GH_TOKEN)` and `The token in GH_TOKEN is invalid.`
- Refreshed the saved GitHub token on 2026-05-28; credential storage and index were updated, and `gh auth status` now confirms login to `github.com` as `koladeg`.
