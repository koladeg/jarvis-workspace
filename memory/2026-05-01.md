- Robin private-chat reliability work: switched the listener to use rolling fresh `research` agent sessions (daily rollover) instead of endlessly reusing one bloated session; this preserves continuity briefly without letting context grow unbounded.
- Durable Robin model policy confirmed: keep Codex/OpenAI usage conservative for both Jarvis and Robin — stay on `gpt-5.4` as the normal Codex path and use `gpt-5.5` only deliberately for testing or clearly harder tasks.
- Robin outage diagnosis on 2026-05-01 had two concrete causes: (1) `research` agent session bloat causing long agent timeouts, and (2) Telegram polling conflicts (`HTTP 409 Conflict`) indicating competing consumers of the same bot token/session path.
- Robin listener hardening added a direct fast-path for lightweight check-in messages (e.g. greetings, "Can you hear me?", "Are you there?", "let me know if any issues") so these no longer invoke the heavy agent path.
- Operational note: if Robin still shows Telegram `409 Conflict` errors after the local listener restart, the likely remaining cause is another poller elsewhere using the same Robin bot token; webhook state was confirmed empty locally.
- Friday credentials check on 2026-05-01 confirmed 10/10 credentials present, but NotebookLM `cookies` are expired by 7 days; Sunday AdugboInsure automation remains blocked on fresh NotebookLM auth.
