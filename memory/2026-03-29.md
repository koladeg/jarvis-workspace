# 2026-03-29

- Kolade set a firm rule: never push secrets.
- After a GitHub push was blocked by secret scanning, `config/mcporter.json` was cleaned to use environment-variable placeholders instead of embedded Google OAuth credentials, then the commit was amended and pushed successfully.
- Standing guardrail: before any future push, avoid committing credentials/tokens/client secrets and prefer env vars or credential files.
- Updated truck search criteria: max vehicle age increased from 10 years to 15 years.
- AdugboInsure video 2 branding/publishing workflow was refined and should be reused for future videos:
  - Start from the raw exported MP4.
  - Use the approved AdugboInsure replacement image (not the old banner).
  - Overlay the image on top of the existing video timeline, centered vertically and scaled to full video width.
  - Show the overlay for the first 3 seconds and the last 5 seconds only.
  - Do not append separate intro/outro clips.
  - Do not add new audio; preserve the original video audio only.
  - Before upload, generate and review preview frames at 2 seconds into the video and 2 seconds before the end.
  - Before upload, verify the final exported runtime matches the raw source runtime.
  - Only report upload complete after returning the actual YouTube video ID and link.
- Failure/guardrail from this run: an earlier render accidentally produced a much longer video because the output duration was not hard-limited; future renders must explicitly lock export duration to the source length and verify it before YouTube upload.
- Confirmed YouTube upload for corrected video 2: `AdugboInsure: A POS Agent Guide` → https://www.youtube.com/watch?v=84Gs3aWNJEc (video ID `84Gs3aWNJEc`).
- Approved AdugboInsure branding spec for future videos: use the approved replacement image overlaid on the existing video timeline, scaled to full video width and centered vertically; show it for the first 3 seconds and the last 5 seconds only; preserve original audio; do not append separate intro/outro clips.
- New AdugboInsure source video downloaded from Drive on 2026-03-29: `The_AdugboInsure_Story.mp4`.
- Confirmed YouTube upload for `The AdugboInsure Story` after applying the approved overlay workflow: https://www.youtube.com/watch?v=QXT6pZ7c2n8 (video ID `QXT6pZ7c2n8`).
- Reusable automation was formalized in `skills/adugboinsure-video-pipeline/` with a one-command script `scripts/run-approved-video-pipeline.sh` to render the approved overlay treatment, verify runtime, generate previews, upload to YouTube, and return the real video ID/link.
- Remaining improvement: the new pipeline script successfully renders and verifies duration, but its upload-result parsing can fail at the final step; tighten the script so the whole pipeline completes cleanly in one pass without a manual retry.
- Weekly memory consolidation completed: `MEMORY.md` refreshed for the week of 2026-03-23 to 2026-03-29, `memory/ARCHIVE.md` updated with a new weekly summary, and no additional >14-day daily logs needed compression beyond the already-archived 2026-03-13 entry.
